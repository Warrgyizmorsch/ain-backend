<div class="mb-4"><label class="form-label fw-bold">Group Name</label><input name="name" class="form-control" required maxlength="255" value="{{ old('name', optional($editingGroup)->name) }}" placeholder="Enter group name"></div>
<div class="mb-4"><label class="form-label fw-bold">Description</label><textarea name="description" class="form-control" rows="3" placeholder="Group description">{{ old('description', optional($editingGroup)->description) }}</textarea></div>
<div class="mb-4"><label class="form-label fw-bold">Status</label><select name="status" class="form-select"><option value="1" {{ optional($editingGroup)->status !== 0 ? 'selected':'' }}>Active</option><option value="0" {{ optional($editingGroup)->status === 0 ? 'selected':'' }}>Inactive</option></select></div>
<div><label class="form-label fw-bold">Users</label><select name="user_ids[]" class="form-select" data-control="select2" data-dropdown-parent="#{{ $editingGroup ? 'editGroup'.$editingGroup->id : 'addGroupModal' }}" multiple data-placeholder="Search and select users">@foreach($users as $user)<option value="{{ $user->id }}" {{ $editingGroup && $editingGroup->users->contains('id',$user->id) ? 'selected':'' }}>{{ $user->name }} - {{ $user->email ?: $user->mobile_no }}</option>@endforeach</select><div class="form-text">Multiple users can be selected.</div></div>
